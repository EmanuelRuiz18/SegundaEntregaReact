import CartImage from "../images/CartImage.svg"

const CartWidget = () => {
    return(
        < >
            <div>
            <img src={CartImage} />
            </div>
        </>
    )
};

export default CartWidget;